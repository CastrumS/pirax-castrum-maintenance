Read-only installed ZIP source audit (no live site or PHP changes):
- Fluent Forms 6.2.14 assets/js/form-submission.js resolves fluentFormVars.ajaxUrl or instance fluent_form_<data-form_instance>.ajaxUrl; POST to that URL with ?t=<Date.now()>; action=fluentform_submit, form_id, data=serialized form. Success .ff-message-success is inserted after the form; error stack is inside its parent. hCaptcha/Turnstile use .h-captcha/.cf-turnstile. Token generation uses a separate mutating action and is deliberately NOT authorized.
- Gravity Forms supplied licensed ZIP assets/js/dist/scripts-theme.min.js uses modes postback, iframe and ajax from #gform_submission_method_<id>. Modern ajax POSTs FormData(form) to gform_theme_config.common.form.ajax.ajaxurl with action=gform_submit_form and form_id (plus native nonce/reference fields). This is separate from gform_get_config/gform_validate_config, which are NOT authorized.
- Added genuine Chromium red cases for FF timestamp URL and GF AJAX route before implementing these exact allowlist branches. No browser-bypassing POST was added to production.
- The helper's published GF contract explicitly excludes save-and-continue (gform_save skips validation). Added a genuine Chromium red case for a click handler setting that draft flag, then a last-moment request-body guard so it cannot spend submission authorization. Preinspection also rejects active draft flags and non-submit GF submission-type controls.
- Native plugin integration remains the next worker's responsibility. Local fixtures exercise route authorization and DOM outcome association, not native plugin correctness/authentication/delivery.
- Temporary extracted source files removed; no licensed plugin ZIP/source retained in evidence.
